// Default layout — swaps the built-in tab-bar for the zellaude plugin,
// which shows Claude Code / Codex CLI activity per tab.
layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="file:~/.config/zellij/plugins/zellaude.wasm"
        }
        children
    }
}
